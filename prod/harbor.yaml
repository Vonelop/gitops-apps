apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor-prod
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://helm.goharbor.io
    chart: harbor
    targetRevision: '*'
    helm:
      releaseName: harbor-prod
      values: |
        expose:
          tls:
            enabled: false
          ingress:
            hosts:
              core: docker-registry.ailabtools.org
            annotations:
              nginx.ingress.kubernetes.io/x-forwarded-proto: "https"
        
        externalURL: https://docker-registry.ailabtools.org
        
        persistence:
          enabled: false
        
        harborAdminPassword: "Stb2W1wQcktWnNkkypU7Csf"

  destination:
    server: https://kubernetes.default.svc
    namespace: harbor-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
